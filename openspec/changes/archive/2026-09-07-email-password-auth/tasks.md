# Tasks: email-password-auth

## 1. Data platform (schema + repositories)

- [x] 1.1 Add the D1 migration and schema for real accounts: purge anonymous rows (`DELETE FROM accounts WHERE email LIKE '%@placeholder.local'`), add `accounts.password_hash` (TEXT, nullable column; application layer enforces non-empty on login) and `accounts.email_verified_at` (nullable TEXT), create the unique index on `lower(email)`, and create the `email_tokens` table (id, accountId FK, tokenHash, purpose CHECK IN ('verify_email','password_reset'), expiresAt, usedAt nullable, createdAt). Update `schema.ts` to match; extend `AccountRepository` abstract + D1 implementation with `findByEmail(email)`, `setPasswordHash(userId, hash)`, and a real `setVerifiedEmail`/verification-state write; add an `EmailTokenRepository` (create, findActiveByTokenHashAndPurpose, consume marking `usedAt`, invalidateAllForAccount). Regenerate the committed Drizzle migration in `packages/data-platform/drizzle/` and update existing repository unit tests. <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/data-platform/src/d1/schema.ts, packages/data-platform/drizzle/**, packages/data-platform/src/repositories/**, packages/data-platform/src/abstracts.ts] -->

## 2. API Worker (production auth surface)

- [x] 2.1 Create the password hashing port `apps/api-worker/src/auth/password.ts`: PBKDF2-SHA256 via WebCrypto, 600 000 iterations, 16-byte random salt, storage format `pbkdf2-sha256$<iter>$<salt-b64url>$<hash-b64url>`, `hashPassword`/`verifyPassword` with a constant-time (XOR-fold) comparison, plus password-policy validation (min 12, max 128 chars, no composition rules). Known-answer unit tests (fixed salt/iteration vectors) that run in Vitest node. <!-- agent: platform-engineer.build, depends_on: [], touches: [apps/api-worker/src/auth/password.ts, apps/api-worker/src/auth/__tests__/password.test.ts] -->
- [x] 2.2 Implement register/login/me in the API Worker: `POST /api/v1/account/register` (validate email + password, lowercase-normalize, unique-email rejection, create account, issue session via the existing SessionTokenService mechanics, best-effort verification email), `POST /api/v1/account/login` (uniform 401 for unknown email vs wrong password, empty-hash fail-safe), `GET /api/v1/account/me` (sessionAuth, `{userId, email, verified}`). Delete the anonymous `POST /api/v1/account/session` issuance route and its handler. Add an `AUTH` rate-limit profile to the limiter registry and apply it to register/login/reset-request; update the guard-middleware table in `guards.ts`; append register/login (success + failure) security events to `audit_events` via `D1AuditEventRepository` (never log passwords or raw tokens). Route tests covering happy paths, enumeration-identical 401s, duplicate email, and rate-limit composition order. <!-- agent: platform-engineer.build, depends_on: [1.1, 2.1], touches: [apps/api-worker/src/routes/accounts.routes.ts, apps/api-worker/src/routes/auth.helpers.ts, apps/api-worker/src/middleware/guards.ts, apps/api-worker/src/middleware/rate-limit.ts, apps/api-worker/src/adapters/account-store.ts, apps/api-worker/src/routes/__tests__/accounts-analytics.routes.test.ts] -->
- [x] 2.3 Implement the email-token flows: `POST /api/v1/account/verify-email/request` (sessionAuth, re-send), `POST /api/v1/account/verify-email/confirm` (token is the capability; sets `email_verified_at`, single-use via `usedAt`, 24 h expiry), `POST /api/v1/account/password/reset-request` (always 202; mail only when the account exists, 1 h expiry), `POST /api/v1/account/password/reset` (rehash, revoke all of the account's sessions, invalidate outstanding reset tokens). Delete the old self-asserted `POST /api/v1/account/verify-email` endpoint. Dispatch mail through the existing email-worker `/internal/email/send` contract (same client pattern as the freshness-alert cron; FI + EN plain-text/HTML bodies; frontend links `/account/verify?token=…`, `/account/reset?token=…`); mail failures log and never fail registration. Route tests with a stubbed email transport covering replay, expiry, cross-purpose token confusion, and session revocation on reset. <!-- agent: platform-engineer.build, depends_on: [1.1, 2.1], touches: [apps/api-worker/src/routes/accounts.routes.ts, apps/api-worker/src/services/email-token.service.ts, apps/api-worker/src/adapters/email-token-store.ts, apps/api-worker/src/routes/__tests__/email-token.routes.test.ts] -->
- [x] 2.4 Remove placeholder-identity semantics from the API Worker: delete `PLACEHOLDER_EMAIL_SUFFIX`-equivalent logic and the derived-from-placeholder `verified` computation; `verified` now maps `email_verified_at IS NOT NULL`; update `authenticated-account.ts`, entitlement mapping, session-response building, and any tests asserting placeholder behaviour (guard-test-harness fixtures use real emails). <!-- agent: platform-engineer.build, depends_on: [2.2], touches: [apps/api-worker/src/auth/authenticated-account.ts, apps/api-worker/src/middleware/session-auth.ts, apps/api-worker/src/middleware/entitlement.ts, apps/api-worker/src/middleware/__tests__/session-auth.test.ts, apps/api-worker/src/middleware/__tests__/guard-test-harness.ts] -->

## 3. Frontend

- [x] 3.1 Replace the anonymous bootstrap with auth pages: delete `issueSession`/`issueSessionOnce` and the 401 auto-mint retry in `lib/api.ts` (account-scoped 401 now surfaces for a login redirect); `ensureSession` calls `GET /api/v1/account/me`; extend `SessionStatus` to `{userId, email, verified}`. New pages `/login`, `/register`, `/account/verify` (lands from the email link, calls confirm, renders result), `/account/forgot`, `/account/reset`; auth-aware `SiteHeader` (Kirjaudu/Rekisteröidy signed-out; account link + logout signed-in); remove the `/account/create` anonymous-account page. FI (default) + EN message-catalog entries for every new string; update `lib/__tests__/api.session.test.ts` and page tests. <!-- agent: platform-engineer.build, depends_on: [2.2, 2.3], touches: [apps/frontend/src/lib/api.ts, apps/frontend/src/lib/types.ts, apps/frontend/src/app/[locale]/login/**, apps/frontend/src/app/[locale]/register/**, apps/frontend/src/app/[locale]/account/**, apps/frontend/src/app/[locale]/components/SiteHeader.tsx, apps/frontend/src/messages/**, apps/frontend/src/lib/__tests__/**] -->

## 4. Legacy NestJS harness

- [x] 4.1 Clean the legacy harness (`packages/application-api` + `apps/backend`, test substrate only): remove the anonymous session-issuance endpoint from `SessionController`, the placeholder-email helpers (`isPlaceholderEmail`/`PLACEHOLDER_EMAIL_SUFFIX` usage) and derived verification from `email-verification.ts`, and the self-asserted verify-email upgrade path; keep `SessionTokenService`, `SessionAuthGuard`, and the account/retention modules functioning for the legacy pg suites; fix affected harness tests. Document the divergence (credentials auth lives only in the API Worker) in a short module docblock — do not port the credential flow into the harness. <!-- agent: platform-engineer.build, depends_on: [1.1], touches: [packages/application-api/src/accounts/**, apps/backend/src/**, packages/application-api/src/accounts/__tests__/**] -->

## 5. Specs and documentation

- [x] 5.1 Apply the spec deltas in this change (`specs/session-authentication`, `specs/accounts-age-gate`) against the live specs, and reconcile any remaining anonymous-account statements elsewhere in `openspec/specs/` (e.g. `application-api` module-surface wording, accounts mentions in `web-application`) so no active spec still describes anonymous issuance, placeholder identities, or PostgreSQL as the production account store. <!-- agent: platform-engineer.fast, depends_on: [], touches: [openspec/specs/session-authentication/spec.md, openspec/specs/accounts-age-gate/spec.md, openspec/specs/application-api/spec.md, openspec/specs/web-application/spec.md] -->
- [x] 5.2 Update prose docs: ARCHITECTURE.md (auth description replaces anonymous-session wording, D1 table list +1 `email_tokens`, email-worker usage now includes verification/reset mail, note the legacy-harness divergence), README.md, docs/TECHNICAL-ASSESSMENT.md (limitation restated: credentials auth exists, no OIDC provider, age gate still documented self-attestation), docs/USER-GUIDE.md (register/login/reset walkthrough, password policy, unverified-badge semantics), docs/cutover-runbook.md (email-worker env vars already required by alert mail; verification/reset mail reuses them). Keep the operator-console restart-volatile limitation verbatim (unchanged follow-up). <!-- agent: platform-engineer.fast, depends_on: [], touches: [ARCHITECTURE.md, README.md, docs/TECHNICAL-ASSESSMENT.md, docs/USER-GUIDE.md, docs/cutover-runbook.md] -->

## 6. Verification

- [x] 6.1 Run the full check battery and fix fallout: `pnpm lint`, workspace typecheck, unit suites, D1 suite (`vitest.config.d1.ts`), api-worker e2e (`vitest.config.e2e.ts` + api-worker e2e incl. route-coverage), OpenNext frontend build + compile check, and `wrangler deploy --dry-run` for api-worker and email-worker. Verify the guard-table route inventory matches the registered routes after the issuance deletion and that no test still exercises the removed endpoints. <!-- agent: platform-engineer.fast, depends_on: [2.2, 2.3, 2.4, 3.1, 4.1, 5.1], touches: [] -->

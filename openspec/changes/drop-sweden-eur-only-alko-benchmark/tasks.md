# Drop Sweden, EUR-only, Alko benchmark — Tasks

> Clean cut of the Systembolaget feed and all Sweden-facing content, full removal of the FX machinery, and a display-only Alko benchmark on calculator results. Most tasks go to `platform-engineer` (TypeScript, Hono, Drizzle/D1, React scope); `devops-engineer` covers the wrangler/infra cron declarations. `fullstack-engineer` is the primary planning agent, not a spawned worker. FX-core tasks (1.1–1.5) parallelize; the schema work (2.1) waits for every writer and reader of the removed columns to be gone, and the benchmark chain (4.x) is independent until it touches the calculator in 4.2.

---

## 1. FX machinery removal

- [x] 1.1 Delete the core-domain FX module (`packages/core-domain/src/fx/**`: service, types, rate-window, ports, tests) and remove `FxModule` registrations from the Nest composition root and the api-worker bridge <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/core-domain/src/fx/**, apps/backend/src/app.module.ts, apps/api-worker/src/adapters/core-domain-bridge.ts] -->
- [x] 1.2 Delete the ECB rate source and the FX dataset review service/adapters in data-acquisition (`ecb-rate.source.ts`, FX review service, fx pipeline adapters) and their exports from the package index <!-- retry complete: application-api jobs consumer removed against fresh dist; pg adapter forwarding added --> <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/data-acquisition/src/adapters/ecb-rate.source.ts, packages/data-acquisition/src/services/**, packages/data-acquisition/src/index.ts] -->
- [x] 1.3 Delete the `fx-dataset-review` cron handler and remove its entry from `cron/router.ts` <!-- retry complete: cron tests cleaned; wrangler parity test goes green with 5.2 -->
- [x] 1.4 Remove the FX dataset version from idempotency cache-key composition in the Nest `IdempotencyService` and the api-worker `IdempotencyDO`; keep tax and transport version components and their tests green <!-- agent: platform-engineer.build, depends_on: [1.1], touches: [packages/application-api/src/idempotency/**, apps/api-worker/src/do/idempotency.do.ts] -->
- [ ] 1.5 Remove the FX dataset publish flow from the operator console: API routes, ops service methods, the console UI section, and their tests; governance grants, tax confirmations, and the correction queue stay <!-- REOPENED (browser-e2e boot finding): 1.5 also removed the `DataPlatformModule` import from `OpsModule`, but `OpsGovernanceService` injects `MerchantRegistryRepository` (exported by DataPlatformModule) — Nest bootstrap of apps/backend now fails with UnknownDependenciesException. Restore the import (DataPlatformModule is FX-free since 2.1). --> <!-- agent: platform-engineer.build, depends_on: [1.1], touches: [apps/api-worker/src/routes/ops.routes.ts, packages/application-api/src/ops/**, apps/frontend/src/app/[locale]/ops/**] -->
- [x] 1.6 Delete `SystembolagetFeedAdapter`, its tests, and its data-acquisition registry/DI wiring so the adapter registry holds Alko only <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/data-acquisition/src/adapters/systembolaget.adapter.ts, packages/data-acquisition/src/__tests__/systembolaget.adapter.test.ts, packages/data-acquisition/src/index.ts] -->
- [x] 1.7 Rewire the api-worker ingestion path to a no-FX, Alko-only composition: `queues/pipeline.ts`, `workflows/ingestion-steps.ts`, `core-domain-bridge.ts`, `d1-upsert.repository.ts`, and `env.ts` drop all FX and Systembolaget references <!-- retry complete after 2.1/3.1 schema drop --> <!-- agent: platform-engineer.build, depends_on: [1.1, 1.2, 1.6], touches: [apps/api-worker/src/queues/pipeline.ts, apps/api-worker/src/workflows/ingestion-steps.ts, apps/api-worker/src/adapters/**, apps/api-worker/src/env.ts] -->

## 2. Schema, seed, and data

- [x] 2.1 Forward migrations dropping `fx_rate_datasets` and `fx_rates` and the `retail_offers` FX provenance columns (`original_price_cents`, `original_currency`, `fx_dataset_version`) on both Postgres (Drizzle migration) and D1 (table rebuild if `DROP COLUMN` constraints require); the `currency` column stays pinned to EUR <!-- agent: platform-engineer.build, depends_on: [1.7], touches: [packages/data-platform/src/schema.ts, packages/data-platform/src/d1/**, packages/data-platform/drizzle/**] -->
- [x] 2.2 Update the pg and D1 seeds and `scripts/seed-d1.ts` (merchant registry without systembolaget, no SEK fixture rows) and add an idempotent purge SQL script deleting systembolaget products, retail offers, price-history summaries, registry rows, and governance records; R2 observation log untouched <!-- agent: platform-engineer.build, depends_on: [2.1], touches: [scripts/seed-d1.ts, packages/data-platform/src/seed/**, infra/staging-data/**, scripts/**] -->
- [x] 2.3 Rename `systembolaget` merchant fixtures to a neutral id across application-api tests (search, merchants, analytics, ops, outbound redirect) <!-- retry complete: excludedOffers fixture residue removed --> <!-- agent: platform-engineer.fast, depends_on: [], touches: [packages/application-api/src/**/__tests__/**] -->

## 3. Currency and event-calculator invariants

- [ ] 3.1 Collapse the currency union to the `'EUR'` literal, delete the unconvertible-offer exclusion path in the calculator and data mapping, and add the data-quality invariant that every stored offer is EUR <!-- REOPENED (6.2 finding): apps/backend/tests/e2e/calculator.test.ts still asserts removed `excludedOffers` (2 failures) — update stale assertions to the new contract --> <!-- agent: platform-engineer.build, depends_on: [1.7], touches: [packages/core-domain/src/calculator/**, packages/data-acquisition/src/services/data-mapping.service.ts, packages/data-platform/src/schema.ts, tests/] -->
- [x] 3.2 Remove `'SE'` from the event calculator: `SOURCING_COUNTRY_ORDER`, country validation, sourcing types, fixtures, and tests <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/core-domain/src/eventcalc/**] -->

## 4. Alko benchmark

- [x] 4.1 New pure core-domain benchmark module (`packages/core-domain/src/benchmark/`): compare the calculated offer against the product's Alko reference offers; output price, difference in euros and percent, reliability status, and `observedAt`, or an explicit unavailable result; unit tests with exact numeric vectors <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/core-domain/src/benchmark/**] -->
- [x] 4.2 Calculator enrichment: resolve the Alko reference through the existing product-data port, add the optional `alkoBenchmark` field to the result DTO and the persisted calculation record (absent when no reference; absent on pre-change records) <!-- retries complete: observedAt read sides + D1 persisted-record round-trip (pg forwarding tracked under 1.2 reopen) --> <!-- agent: platform-engineer.build, depends_on: [4.1, 3.1], touches: [packages/core-domain/src/calculator/**, apps/api-worker/src/routes/calculator.routes.ts, packages/application-api/src/calculator/**] -->
- [x] 4.3 Result UI benchmark line with fi/en catalog copy: factual wording only, passes content lint, renders nothing when the field is absent <!-- agent: platform-engineer.build, depends_on: [4.2], touches: [apps/frontend/src/app/[locale]/calculator/components/CalculatorResult.tsx, apps/frontend/src/messages/**] -->
- [x] 4.4 Compliance test extending the neutrality suite: results byte-identical with zero, one, and many Alko reference rows; benchmark excluded from totals, breakdown, and every ranking input <!-- agent: platform-engineer.build, depends_on: [4.2], touches: [tests/compliance/**] -->

## 5. Copy and infrastructure config

- [x] 5.1 Rewrite hero and trust-row copy (fi/en) without Sweden/Systembolaget naming; drop the Swedish country-label entries; keep content-lint translation coverage green in both locales <!-- agent: platform-engineer.fast, depends_on: [], touches: [apps/frontend/src/messages/fi.json, apps/frontend/src/messages/en.json] -->
- [x] 5.2 Remove the fx cron pattern from `wrangler.jsonc` in all environments and update the cron and capability descriptions in `infra/environments/*.yaml` <!-- agent: devops-engineer.fast, depends_on: [1.3], touches: [apps/api-worker/wrangler.jsonc, infra/environments/**] -->
- [x] 5.3 Update documentation to the EUR-only, no-Sweden reality: README, ARCHITECTURE.md, DESIGN.md trust row, USER-GUIDE.md, the legal briefing and RFP template (generic EUR market wording), and curation-criteria <!-- agent: platform-engineer.fast, depends_on: [1.7, 3.1, 3.2], touches: [README.md, ARCHITECTURE.md, DESIGN.md, docs/USER-GUIDE.md, docs/legal-*.md, docs/curation-criteria.md] -->

## 6. Verification

- [x] 6.1 Leftover-reference sweep: zero non-archive hits for `systembolaget`, `SEK`, and `ecb-rate` across code, seeds, and message catalogs; document any intentional historical-doc mentions <!-- agent: platform-engineer.fast, depends_on: [2.2, 2.3, 3.1, 3.2, 4.3, 5.1, 5.2], touches: [] -->
- [ ] 6.2 Full verification: typecheck, lint, `lint:content`, unit, golden, data-quality, compliance, e2e, D1 suites, browser e2e journeys, plus a local `dev-up.sh` smoke of search, calculate, and the benchmark line <!-- agent: platform-engineer.build, depends_on: [6.1], touches: [] -->

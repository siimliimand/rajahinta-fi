# Tasks: insight-surfaces

## 1. Data platform (schema + repositories)

- [x] 1.1 Add the `savingsSnapshots` D1 migration and schema: asOf date, productId, category, best-offer fields (merchant, merchantCountry, priceCents, observedAt), alkoReferenceCents, alkoObservedAt, landedTotalCents, landedReliability, confidence, gapCents, gapBasisPoints, taxDatasetVersion; `unique(asOf, productId)`; CHECK constraints on reliability and confidence value sets. Abstract repository + D1 implementation (idempotent per-day keyed upsert, latest-day and per-category range reads), registered in the platform module; unit tests on the D1 harness. <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/data-platform/src/d1/schema.ts, packages/data-platform/drizzle/**, packages/data-platform/src/repositories/**] -->
- [x] 1.2 Add the `blogPosts.kind` migration (TEXT NOT NULL DEFAULT 'RATE_CHANGE', CHECK IN ('RATE_CHANGE','GUIDE')); extend the blog repository with kind-aware create and kind-filtered list queries (slug uniqueness per (slug, locale) unchanged, existing rows keep RATE_CHANGE via the default); unit tests. <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/data-platform/src/d1/schema.ts, packages/data-platform/drizzle/**, packages/data-platform/src/repositories/d1/blog-post.repository.ts, packages/data-platform/src/repositories/d1/__tests__/**] -->

## 2. Savings discovery

- [x] 2.1 Create `packages/core-domain/src/savings/`: pure gap computation (landed total vs Alko reference; gap in cents and basis points; explicit unavailable reasons mirroring the unit-price result pattern), deterministic ordering (gap basis points descending, product name ascending tiebreaker), status-carrying row types; unit tests; export from the package index. <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/core-domain/src/savings/**, packages/core-domain/src/index.ts] -->
- [x] 2.2 Aggregation cron pass: after time-series aggregation in the cron handler group (own `waitUntil`, off the request path), evaluate products carrying an Alko reference offer with an observation timestamp, run `LandedCostCalculatorService` per product (quantity 1, destination FI, default arrangement), materialize the day's `savingsSnapshots` idempotently with per-product failure isolation and a cadence constant; unit tests incl. re-run idempotence and per-product isolation. <!-- agent: platform-engineer.build, depends_on: [1.1, 2.1], touches: [apps/api-worker/src/cron/**] -->
- [x] 2.3 API route `GET /api/v1/savings` (public, age gate, own rate-limit profile; category and limit params; as-of date; coverage counts: evaluated / with reference / listed; reliability and confidence per row); route tests incl. empty-category zero state. <!-- agent: platform-engineer.build, depends_on: [2.2], touches: [apps/api-worker/src/routes/**, apps/api-worker/src/index.ts] -->
- [x] 2.4 Frontend `/savings` page mirroring the `/value` pattern: category selector, rows with landed total, Alko reference, gap, reliability badges, as-of and coverage counts in the header, factual informational copy stating the ordering rule, honest zero state; sitemap inclusion; FI + EN catalogs. <!-- agent: platform-engineer.build, depends_on: [2.3], touches: [apps/frontend/src/app/[locale]/savings/**, apps/frontend/src/messages/**, apps/frontend/src/app/sitemap.ts] -->

## 3. Price context

- [x] 3.1 Create `packages/core-domain/src/price-context/`: pure computation over a daily bucket series (window median/min/max, delta versus median in cents and basis points, minimum-bucket constant, status-carrying value | unavailable-with-reason `INSUFFICIENT_HISTORY` mirroring the unit-price result shape); unit tests; export from the package index. <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/core-domain/src/price-context/**, packages/core-domain/src/index.ts] -->
- [x] 3.2 API route `GET /api/v1/products/:id/price-context` (public, age gate, historical rate-limit profile; 90-day product-wide merchant-null bucket window; current best price from the same lowest-current-offer rule the product page uses, extracted into one shared helper; window, bucket count, as-of in every payload); route tests incl. insufficient-history state and best-price consistency with the product detail response. <!-- agent: platform-engineer.build, depends_on: [3.1], touches: [apps/api-worker/src/routes/**, apps/api-worker/src/index.ts] -->
- [x] 3.3 Product page context line rendering the delta versus the 90-day median with window and as-of, insufficient-history copy, factual phrasing only; FI + EN catalogs; content-lint additions banning advice phrasing ("best deal", "good time to buy", "buy now") across the new surfaces. <!-- agent: platform-engineer.build, depends_on: [3.2], touches: [apps/frontend/src/app/[locale]/products/**, apps/frontend/src/messages/**] -->

## 4. Allowance explorer

- [x] 4.1 API routes `GET /api/v1/allowances?date=YYYY-MM-DD` (published dataset effective on the date via `findPublishedEffectiveOn`, limits with verbatim citations, version label, effective window; ISO date validation; explicit not-found when no version covers the date) and `GET /api/v1/allowances/versions` (published version history with windows); public, age gate, rate-limit profile; strictly read-side; route tests incl. cross-version date resolution, unpublished exclusion, and no-date case. <!-- agent: platform-engineer.build, depends_on: [], touches: [apps/api-worker/src/routes/**, apps/api-worker/src/index.ts] -->
- [x] 4.2 Frontend `/allowances` page: date picker defaulting to today, per-category caps with citations rendered verbatim as evidence links, version label + effective window next to the caps, version-history section, standing guidance-not-legal-advice copy; sitemap inclusion; FI + EN catalogs. <!-- agent: platform-engineer.build, depends_on: [4.1], touches: [apps/frontend/src/app/[locale]/allowances/**, apps/frontend/src/messages/**, apps/frontend/src/app/sitemap.ts] -->

## 5. Guides hub

- [x] 5.1 Guides publication backend: ops routes to create/edit GUIDE-kind drafts and publish through the existing human DRAFT-to-PUBLISHED path (OpsAccessGuard, audited; guides carry no rate-version provenance), kind-filtered public listing endpoints (blog index RATE_CHANGE only, guides listing GUIDE only), content-lint coverage for guide bodies in both locales; route tests. <!-- agent: platform-engineer.build, depends_on: [1.2], touches: [apps/api-worker/src/routes/**, packages/core-domain/src/content/**, apps/frontend/src/app/[locale]/ops/**] -->
- [x] 5.2 Frontend `/guides` index and `/guides/[slug]` (PUBLISHED GUIDE posts only, locale-aware, sitemap inclusion), cross-links to `/allowances` and `/trip` where topically relevant, ops guide draft editor pages; FI + EN catalogs. <!-- agent: platform-engineer.build, depends_on: [5.1], touches: [apps/frontend/src/app/[locale]/guides/**, apps/frontend/src/app/[locale]/ops/**, apps/frontend/src/messages/**, apps/frontend/src/app/sitemap.ts] -->

## 6. Compliance + verification

- [x] 6.1 Compliance tests: savings isolation (calculator, ranking, and basket outputs byte-identical with zero, one, and many savings snapshots present, mirroring `tests/compliance/accuracy-unitprice-input-isolation.test.ts`), allowance-explorer read-only (exercising the endpoints and page data paths cannot create, modify, or unpublish any PUBLISHED dataset row), price-context payload carries window + bucket count + as-of on every value-bearing response. <!-- agent: platform-engineer.build, depends_on: [2.3, 3.2, 4.1], touches: [tests/compliance/**] -->
- [x] 6.2 Full verification run: typecheck, lint, content lint, unit, golden, data-quality, compliance, e2e, api-worker e2e, OpenNext build; fix fallout. <!-- agent: platform-engineer.fast, depends_on: [2.4, 3.3, 4.2, 5.2, 6.1], touches: [] -->

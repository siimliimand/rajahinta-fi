# Tasks: trust-and-reach-roadmap

## 1. Data platform (schema + repositories)

- [x] 1.1 Add the trust-tables D1 migration and schema: `shopReports` (merchantDomain, merchantNameNormalized, orderReference, correspondenceSummary, reporterAccountId FK, status CHECK IN ('OPEN','LINKED','REJECTED'), linkedEntryId nullable, createdAt), `blacklistEntries` (merchantDomain, merchantNameNormalized, standardMet TEXT, publishedAt, publishedBy, status CHECK IN ('PUBLISHED','REOPENED','REJECTED'), appeal fields nullable), `calculationOutcomes` (calculationRecordId, account snapshot digest of estimate fields, estimatedTotalCents, reportedTotalCents, reportedAt, unique(calculationRecordId, reporterAccountId)), with `calculationOutcomes` excluded from the calculation-record retention sweep and given its own 24-month cap constant. Update `schema.ts` and regenerate the committed migration. <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/data-platform/src/d1/schema.ts, packages/data-platform/drizzle/**] -->
- [x] 1.2 Add the content/share/alert D1 migration and schema: `blogPosts` (slug, locale, title, bodyMarkdown, status CHECK IN ('DRAFT','PUBLISHED'), rateDatasetVersion nullable, publishedAt nullable, createdAt, unique(slug, locale)), `newsletterSubscribers` (email unique lower-index, status CHECK IN ('PENDING','ACTIVE','UNSUBSCRIBED'), confirmationTokenHash, confirmedAt nullable, unsubscribedAt nullable), `shareSnapshots` (publicId unique 22-char, frozen result JSON, createdAt), and `priceAlerts.kind` (TEXT NOT NULL DEFAULT 'PRICE' CHECK IN ('PRICE','TAX_CHANGE')). Update `schema.ts` and regenerate the committed migration; existing alert rows default to PRICE. <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/data-platform/src/d1/schema.ts, packages/data-platform/drizzle/**] -->
- [x] 1.3 Add repositories for the trust tables: `ShopReportRepository`, `BlacklistRepository`, `CalculationOutcomeRepository` (abstract + D1 implementations, registered in the platform module), with outcome read-side aggregation helpers (count, within-margin share by period). Unit tests against the D1 harness. <!-- agent: platform-engineer.build, depends_on: [1.1], touches: [packages/data-platform/src/repositories/**, packages/data-platform/src/abstracts.ts, packages/data-platform/src/data-platform.module.ts] -->
- [x] 1.4 Add repositories for `blogPosts`, `newsletterSubscribers`, `shareSnapshots` and extend the alert repository with kind-aware create/list (duplicate check now per product+kind). Register in the platform module; unit tests against the D1 harness. <!-- agent: platform-engineer.build, depends_on: [1.2], touches: [packages/data-platform/src/repositories/**, packages/data-platform/src/abstracts.ts, packages/data-platform/src/data-platform.module.ts] -->

## 2. Shop blacklist

- [x] 2.1 Create `packages/core-domain/src/blacklist/`: evidence validation, report state machine (OPEN → linked to entry on publication; appeal moves entry PUBLISHED → REOPENED → PUBLISHED/REJECTED), published-standard constants (3+ independent confirmed non-delivery reports OR confirmed invalid business registration), merchant identity normalization (domain + normalized name). Pure domain logic + unit tests; export from the package index. <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/core-domain/src/blacklist/**, packages/core-domain/src/index.ts] -->
- [x] 2.2 API worker routes: `POST /api/v1/reports` (sessionAuth, evidence validation, AUTH-style rate-limit profile, audit event), and additive `merchantWarnings` blocks joined into product detail, search, and compare responses for merchants with PUBLISHED entries (domain/name match). Warnings must be additive only; add route tests asserting offer arrays and totals unchanged with warnings present and absent. <!-- agent: platform-engineer.build, depends_on: [1.3, 2.1], touches: [apps/api-worker/src/routes/**, apps/api-worker/src/adapters/**] -->
- [x] 2.3 Ops console moderation surface: report review queue (list OPEN with evidence, link/reject), publish action enforcing the published standard server-side, appeal inbox with resolve actions, all behind OpsAccessGuard and appended to the audit trail; ops frontend pages for the same. <!-- agent: platform-engineer.build, depends_on: [2.2], touches: [apps/api-worker/src/routes/ops*.ts, apps/frontend/src/app/[locale]/ops/**] -->
- [x] 2.4 Frontend warning badges: shared warning component (standard status/design tokens) rendered on product, compare, and search surfaces wherever a warned merchant appears, linking the ranking-methodology page; FI + EN message catalogs. <!-- agent: platform-engineer.build, depends_on: [2.2], touches: [apps/frontend/src/app/[locale]/**, apps/frontend/src/messages/**] -->

## 3. Verified outcomes

- [x] 3.1 Create `packages/core-domain/src/outcomes/`: submission validation (60-day window from record timestamp, one per record+account, positive cents, owns-record check contract), margin comparison (within 5% of estimatedTotalCents), pure aggregation function (count, withinMarginShare, asOf), all labeled user-reported. Unit tests including empty-state and boundary windows; export from the package index. <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/core-domain/src/outcomes/**, packages/core-domain/src/index.ts] -->
- [x] 3.2 API worker routes: `POST /api/v1/calculations/:id/outcome` (sessionAuth, owns-record, window + duplicate enforcement), `GET /api/v1/accuracy` (public, count + share + as-of + user-reported label), and history payload extension flagging records still missing outcomes for the in-account prompt. Route tests for window boundaries, duplicates, ownership, and empty accuracy. <!-- agent: platform-engineer.build, depends_on: [1.3, 3.1], touches: [apps/api-worker/src/routes/**, apps/api-worker/src/adapters/**] -->
- [x] 3.3 Frontend: accuracy statistic in the home trust row and a methodology-page section (always with sample size and user-reported wording, honest zero state), report-outcome form on account history records within the window, FI + EN catalogs. <!-- agent: platform-engineer.build, depends_on: [3.2], touches: [apps/frontend/src/app/[locale]/**, apps/frontend/src/messages/**] -->

## 4. Tax-change alerts

- [x] 4.1 Extend the cron alert evaluator with the TAX_CHANGE kind: on rate-version confirmation enqueue evaluation, resolve per-product landed-cost deltas via `TaxChangeAttributionService`, select active TAX_CHANGE alerts whose product moved, deliver through the shared intent-log + 24 h cooldown path. Unit tests for selection, cooldown, crash-safe redelivery. <!-- agent: platform-engineer.build, depends_on: [1.2], touches: [apps/api-worker/src/cron/**] -->
- [x] 4.2 Alert API + account UI: create/list with kind=tax_change (no threshold, duplicate check per product+kind), alert management page kind toggle and copy explaining the trigger, FI + EN catalogs. <!-- agent: platform-engineer.build, depends_on: [4.1], touches: [apps/api-worker/src/routes/**, apps/frontend/src/app/[locale]/account/**, apps/frontend/src/messages/**] -->

## 5. Blog + newsletter

- [x] 5.1 Blog service + routes: `packages/core-domain/src/content/` draft builder (what changed, effective date, typical-basket impact from the versioned rate delta, FI + EN bodies), draft creation hooked into the rate-version confirmation path (fail-open, logged), public `GET /api/v1/blog/posts` and `/blog/posts/:slug` returning PUBLISHED only, ops publish endpoint, content-lint coverage for post bodies. <!-- agent: platform-engineer.build, depends_on: [1.4], touches: [packages/core-domain/src/content/**, apps/api-worker/src/cron/**, apps/api-worker/src/routes/**] -->
- [x] 5.2 Frontend blog: `/blog` index and `/blog/[slug]` pages (locale-aware, PUBLISHED only), sitemap inclusion, neutral-tone copy per content lint, FI + EN catalogs. <!-- agent: platform-engineer.build, depends_on: [5.1], touches: [apps/frontend/src/app/[locale]/blog/**, apps/frontend/src/messages/**, apps/frontend/src/app/sitemap.ts] -->
- [x] 5.3 Newsletter backend: subscribe (pending + confirmation token via email worker), confirm, unsubscribe (immediate, one-click link), ops notify-subscribers action writing intent rows before send and marking outcomes after through `/internal/email/send`; FI + EN mail bodies. Route tests with stubbed transport (pending never mailed, crash-safe redelivery). <!-- agent: platform-engineer.build, depends_on: [1.4, 5.1], touches: [apps/api-worker/src/routes/**, apps/email-worker/src/**, apps/frontend/src/app/[locale]/ops/**] -->
- [x] 5.4 Newsletter UI: subscribe form in the blog and footer with explicit consent copy (separate from price alerts), confirmation landing page, FI + EN catalogs. <!-- agent: platform-engineer.fast, depends_on: [5.3], touches: [apps/frontend/src/app/[locale]/**, apps/frontend/src/messages/**] -->

## 6. Share + embed

- [x] 6.1 Snapshot service + routes: `packages/core-domain/src/sharing/` (frozen copy assembly, 22-char random public id, personal-data strip assertion), `POST /api/v1/calculations/:id/share` (sessionAuth, owns-record), public `GET /api/v1/share/:publicId`, 12-month hygiene sweep constant. Route tests incl. frozen-copy semantics and no-account-fields assertion. <!-- agent: platform-engineer.build, depends_on: [1.4], touches: [packages/core-domain/src/sharing/**, apps/api-worker/src/routes/**] -->
- [x] 6.2 Frontend share page `/share/[publicId]`: snapshot render with DisclaimerBanner, generateMetadata OG card from snapshot fields, accuracy-stat cross-link, not-found handling without identifier leakage, FI + EN catalogs. <!-- agent: platform-engineer.build, depends_on: [6.1], touches: [apps/frontend/src/app/[locale]/share/**, apps/frontend/src/messages/**] -->
- [x] 6.3 Embed calculator page `/embed/calculator`: minimal chrome, iframe-friendly response headers, age gate and disclaimers intact, submissions through the normal API path (same guards), following the what-if widget implementation. <!-- agent: platform-engineer.build, depends_on: [], touches: [apps/frontend/src/app/[locale]/embed/**] -->

## 7. €/g ranking

- [x] 7.1 API: `GET /api/v1/unitprice/ranking?category=` — deterministic ORDER BY eurPerGram ASC with stable secondary key, unitprice module reuse, VERIFIED/ESTIMATED rows only, unavailable omitted, status per row. Unit + route tests for determinism, omission, status carriage. <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/core-domain/src/unitprice/**, apps/api-worker/src/routes/**] -->
- [x] 7.2 Frontend value page: category selector, ranking table with standard status badges, informational copy (no best/recommended phrasing, content-lint clean), FI + EN catalogs. <!-- agent: platform-engineer.build, depends_on: [7.1], touches: [apps/frontend/src/app/[locale]/value/**, apps/frontend/src/messages/**] -->

## 8. Allowance-fill planner

- [x] 8.1 Core-domain allowance fill: new objective in the optimizer's bounded search (maximize filled value subject to the traveller-allowance limit resolved from `travellerAllowanceDatasets`/`travellerAllowanceLimits` by effective travel date), response shape mirroring basket optimize with per-line contribution + headroom + dataset-version provenance. Unit tests (date-resolved limits, bound exhaustion) and a load-test case. <!-- agent: platform-engineer.build, depends_on: [], touches: [packages/core-domain/src/tripcalc/**, packages/core-domain/src/optimizer/**] -->
- [x] 8.2 API route: `POST /api/v1/trip/fill` with rate-limit profile, entitlement check consistent with trip calculator, ferry-offer block attached in its separate display-only section. Route tests. <!-- agent: platform-engineer.build, depends_on: [8.1], touches: [apps/api-worker/src/routes/**] -->
- [x] 8.3 Frontend: fill mode on the trip page (allowance input, candidate selection, result itemization), existing ferry-offer block untouched, FI + EN catalogs. <!-- agent: platform-engineer.build, depends_on: [8.2], touches: [apps/frontend/src/app/[locale]/trip/**, apps/frontend/src/messages/**] -->

## 9. Compliance, specs, verification

- [x] 9.1 Compliance suite additions: blacklist warnings byte-identical (zero/one/many) across product/search/compare responses; accuracy statistic and €/g ranking proven not to feed ranking or calculation inputs (import-analysis + output-identity patterns); trip fill byte-identical across ferry-row counts; €/g ordering determinism lockstep. <!-- agent: platform-engineer.build, depends_on: [2.2, 3.2, 4.1, 7.1, 8.2], touches: [tests/compliance/**] -->
- [ ] 9.2 Apply the spec deltas in this change against the live specs (merchant-blacklist, calculation-outcomes, content-publication, share-permalinks, allowance-fill-planner added; price-alerts, unit-price-metrics modified) and update prose docs: ARCHITECTURE.md (new modules, D1 table list, cron handlers, email-worker consumers), README.md (API surface, jobs), docs/USER-GUIDE.md, docs/TECHNICAL-ASSESSMENT.md. <!-- agent: platform-engineer.fast, depends_on: [2.4, 3.3, 4.2, 5.4, 6.2, 6.3, 7.2, 8.3], touches: [openspec/specs/**, ARCHITECTURE.md, README.md, docs/**] -->
- [ ] 9.3 Run the full battery and fix fallout: `pnpm lint`, `pnpm lint:content`, typecheck, unit suites, D1 suite, api-worker e2e, compliance suite, OpenNext frontend build + compile check, `wrangler deploy --dry-run` for api-worker and email-worker; verify the guard-table route inventory covers every new route and rate-limit profile. <!-- agent: platform-engineer.fast, depends_on: [9.1, 9.2], touches: [] -->

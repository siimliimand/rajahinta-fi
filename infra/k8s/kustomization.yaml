---
# Root convenience kustomization.
#
# Apply staging:   kubectl apply -k infra/k8s/overlays/staging
# Apply prod:      kubectl apply -k infra/k8s/overlays/production
#
# This file exists as a navigational aid and is not used directly.
# See overlays/ for environment-specific configurations.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - base
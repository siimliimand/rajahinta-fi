/**
 * Common API plumbing — cross-cutting HTTP concerns shared by every
 * controller (task 3.4, change technical-assessment-remediation).
 *
 * @module CommonIndex
 */

export { ApiErrorFilter } from './api-error.filter';

import { describe, it, expect } from 'vitest';

describe('PipelineOrchestratorService', () => {
  it('skips disabled merchants', async () => {
    // Placeholder — full test suite added with merchant adapter impl
    expect(true).toBe(true);
  });
});
import { describe, it, expect } from 'vitest';

describe('FeedIngestionService', () => {
  it('returns empty result when no adapter is registered', async () => {
    // Placeholder — full test suite added with merchant adapter impl
    expect(true).toBe(true);
  });
});
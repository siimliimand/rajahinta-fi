{
  "name": "@rajahinta-spike/vertical-slice",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "G3 vertical slice spike (migrate-to-cloudflare task 1.3) — calculator endpoint end-to-end in a scratch Worker: Hono + translated D1 schema subset + real core-domain calculator + DO rate limit. Throwaway; outside the pnpm workspace on purpose.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "seed": "wrangler d1 execute spike-db --local --file seed.sql",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs",
    "load": "node scripts/load.mjs"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "drizzle-orm": "^0.44.0",
    "hono": "^4.6.0",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250509.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.14.0"
  }
}
